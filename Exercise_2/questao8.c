#include <stdio.h>

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    if (a %b == 0 ) {
        printf("a é divisivel por b");

    }
    else {
        printf("o a nao é divisilvel por b");

    }
    return 0;
}
